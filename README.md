# Pokemon Stadium Lite Application

1. Ejecutar `npm install` para instalar las dependencias
2. Ejecutar `npm run dev` para levantar el proyecto en modo desarrollo
3. Ejecutar `npm run build` para construir la version para producción del proyecto
4. Ejecutar `npm start` para correr el proyecto en modo producción
